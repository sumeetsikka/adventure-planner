{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(npx tsc:*)",
      "Bash(npm install:*)",
      "Bash(npx tsx:*)",
      "Bash(curl -s http://localhost:3001/api/packages -X POST -H \"Content-Type: application/json\" -d '{}')",
      "Bash(kill %1)",
      "Bash(pkill -f \"tsx server/index\")"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/sumeet/sumeet/Projects/Holiday-Planner && git add -A && git diff --cached --quiet || (git commit -m 'Auto-commit: file changes' --no-verify && git push origin main --quiet 2>/dev/null &)"
          }
        ]
      }
    ]
  }
}
